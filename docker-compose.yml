services:
  db:
    image: mysql:8
    container_name: laboratoriocultural_db
    restart: always
    environment:
      MYSQL_DATABASE: laboratoriocultural_db
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
